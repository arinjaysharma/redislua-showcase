# Port for web server (Render sets PORT automatically)
PORT=8080

# Redis connection string (e.g. from Upstash or Redis Cloud)
# Format: rediss://default:<password>@<host>:<port>
REDIS_URL=

# Alternatively, for local Redis:
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
